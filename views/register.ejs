<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>SGIFY | Register</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://kit.fontawesome.com/yourkitid.js" crossorigin="anonymous"></script>
  <style>
    body {
      background: linear-gradient(135deg, #1d2671 0%, #c33764 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    .auth-container {
      display: flex;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 15px 40px rgba(0,0,0,0.3);
      max-width: 900px;
      background: rgba(255,255,255,0.95);
      backdrop-filter: blur(12px);
      animation: fadeIn 1s ease-in-out;
    }

    .illustration {
      background: linear-gradient(135deg, #1d2671 0%, #c33764 100%);
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 50px;
      transition: transform 0.3s;
    }

    .illustration svg {
      width: 250px;
      height: auto;
    }

    .illustration:hover {
      transform: scale(1.05);
    }

    .form-section {
      padding: 50px 40px;
      width: 100%;
    }

    h2 {
      color: #1d2671;
      font-weight: 700;
      margin-bottom: 40px;
      text-align: center;
    }

    h2 span {
      color: #c33764;
    }

    .form-floating-label {
      position: relative;
      margin-bottom: 1.5rem;
    }

    .form-floating-label input,
    .form-floating-label textarea,
    .form-floating-label select {
      width: 100%;
      padding: 15px 15px 15px 50px;
      border-radius: 10px;
      border: 1px solid #ccc;
      outline: none;
      transition: all 0.3s;
    }

    .form-floating-label input:focus,
    .form-floating-label textarea:focus,
    .form-floating-label select:focus {
      border-color: #c33764;
      box-shadow: 0 0 0 0.25rem rgba(195, 55, 100, 0.25);
    }

    .form-floating-label label {
      position: absolute;
      top: 50%;
      left: 50px;
      transform: translateY(-50%);
      background: transparent;
      padding: 0 5px;
      color: #666;
      pointer-events: none;
      transition: all 0.3s;
    }

    .form-floating-label input:focus + label,
    .form-floating-label input:not(:placeholder-shown) + label,
    .form-floating-label textarea:focus + label,
    .form-floating-label textarea:not(:placeholder-shown) + label,
    .form-floating-label select:focus + label,
    .form-floating-label select:not([value=""]) + label {
      top: -10px;
      left: 45px;
      font-size: 0.8rem;
      color: #c33764;
      background: white;
    }

    .form-floating-label .input-icon {
      position: absolute;
      top: 50%;
      left: 15px;
      transform: translateY(-50%);
      color: #c33764;
    }

    .btn-primary {
      background: linear-gradient(135deg, #c33764, #1d2671);
      border: none;
      transition: all 0.3s ease-in-out;
      font-weight: 600;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(29, 38, 113, 0.4);
    }

    a {
      transition: color 0.3s;
    }

    a:hover {
      color: #c33764 !important;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 991px) {
      .auth-container {
        flex-direction: column;
      }
      .illustration {
        display: none !important;
      }
      .form-section {
        padding: 40px 20px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-10">
        <div class="row auth-container">

          <!-- Illustration Section -->
          <div class="col-lg-6 d-none d-lg-flex illustration">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 640 512" fill="white">
              <path d="M480 224h-64v-96c0-17.7-14.3-32-32-32H256c-17.7 
              0-32 14.3-32 32v96h-64c-35.3 0-64 28.7-64 64v160c0 
              17.7 14.3 32 32 32h384c17.7 0 32-14.3 
              32-32V288c0-35.3-28.7-64-64-64zm-192-96h64v96h-64v-96z"/>
            </svg>
          </div>

          <!-- Form Section -->
          <div class="col-lg-6 col-12 form-section">
            <h2>
              <i class="fas fa-user-plus"></i> Register to <span>SGIFY</span>
            </h2>

            <form action="/register" method="POST">

              <div class="form-floating-label">
                <i class="fas fa-user input-icon"></i>
                <input type="text" name="name" placeholder=" " required>
                <label>Full Name</label>
              </div>

              <div class="form-floating-label">
                <i class="fas fa-envelope input-icon"></i>
                <input type="email" name="email" placeholder=" " required>
                <label>Email</label>
              </div>

              <div class="form-floating-label">
                <i class="fas fa-phone input-icon"></i>
                <input type="tel" name="phonenumber" placeholder=" " required>
                <label>Phone Number</label>
              </div>

              <div class="form-floating-label">
                <i class="fas fa-lock input-icon"></i>
                <input type="password" name="password" placeholder=" " required>
                <label>Password</label>
              </div>

              <div class="form-floating-label">
                <input type="number" name="age" placeholder=" ">
                <label>Age</label>
              </div>

              <div class="form-floating-label">
                <select name="gender">
                  <option value="" selected disabled></option>
                  <option>Male</option>
                  <option>Female</option>
                  <option>Other</option>
                </select>
                <label>Gender</label>
              </div>

              <div class="form-floating-label">
                <input type="text" name="education" placeholder=" ">
                <label>Education</label>
              </div>

              <div class="form-floating-label">
                <input type="text" name="profession" placeholder=" ">
                <label>Profession</label>
              </div>

              <div class="form-floating-label">
                <input type="text" name="skills" placeholder=" ">
                <label>Skills (comma separated)</label>
              </div>

              <div class="form-floating-label">
                <textarea name="goals" rows="2" placeholder=" "></textarea>
                <label>Goals</label>
              </div>

              <button type="submit" class="btn btn-primary w-100 py-2">
                <i class="fas fa-check-circle"></i> Register
              </button>
            </form>

            <div class="text-center mt-3">
              <p>Already have an account? 
                <a href="/login" class="fw-bold">Login</a>
              </p>
            </div>
          </div>

        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
