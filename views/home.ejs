<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SGIFY - Personalized Learning & Career Guidance</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://kit.fontawesome.com/yourkitid.js" crossorigin="anonymous"></script>

  <style>
    /* Full page hero */
    body {
      height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: "Segoe UI", sans-serif;
      background: linear-gradient(135deg, rgba(0,123,255,0.85), rgba(102,16,242,0.85)),
                  url("https://images.unsplash.com/photo-1519389950473-47ba0277781c?auto=format&fit=crop&w=1470&q=80") no-repeat center center/cover;
      background-attachment: fixed;
      color: #fff;
    }

    .hero-card {
      background: rgba(255, 255, 255, 0.95);
      color: #000;
      backdrop-filter: blur(12px);
      border-radius: 1.5rem;
      max-width: 600px;
      padding: 3rem 2rem;
      text-align: center;
      box-shadow: 0 20px 50px rgba(0,0,0,0.2);
      animation: fadeInUp 1s ease-out forwards;
      opacity: 0;
    }

    /* Buttons */
    .btn-hero {
      padding: 0.75rem 2rem;
      font-size: 1.1rem;
      border-radius: 50px;
      transition: all 0.3s ease;
    }

    .btn-hero:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.2);
    }

    /* Subtle fade in animation */
    @keyframes fadeInUp {
      0% { opacity: 0; transform: translateY(20px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    /* Text styles */
    h1.hero-title {
      font-weight: 700;
      margin-bottom: 1rem;
    }

    p.hero-subtitle {
      font-size: 1.15rem;
      margin-bottom: 2rem;
      color: #555;
    }

    /* Responsive adjustments */
    @media (max-width: 576px) {
      .hero-card {
        padding: 2rem 1.5rem;
      }
      .btn-hero {
        width: 100%;
        margin-bottom: 0.75rem;
      }
    }
  </style>
</head>
<body>

  <div class="hero-card">
    <h1 class="hero-title text-primary">🚀 Welcome to SGIFY</h1>
    <p class="hero-subtitle">
      Unlock your potential with personalized learning paths and AI-powered career guidance.
    </p>

    <div class="d-flex justify-content-center gap-3 flex-wrap">
      <a href="/login" class="btn btn-outline-primary btn-hero">
        <i class="fas fa-sign-in-alt me-2"></i> Login
      </a>
      <a href="/register" class="btn btn-primary btn-hero">
        <i class="fas fa-user-plus me-2"></i> Register
      </a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
